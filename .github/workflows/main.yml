name: CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

# Prevent multiple concurrent runs on the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Principle of Least Privilege: explicitly define permissions
permissions:
  contents: read

jobs:
  # Security Gate: Must pass before packaging or publishing
  security:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Secret scanning
        uses: trufflesecurity/trufflehog@main

      - name: SAST (CodeQL Init)
        uses: github/codeql-action/init@v3
        with:
          languages: 'python'

      - name: SAST (CodeQL Analysis)
        uses: github/codeql-action/analyze@v3

  # Parallel Job: Package Vim configuration
  package:
    needs: security
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Package Vim configuration
        run: tar -czf vim.tar.gz .vim .vimrc

      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: vim-package
          path: vim.tar.gz

  # Parallel Job: Generate Documentation Page
  pages:
    needs: security
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Generate HTML from README
        run: |
          mkdir public
          npx markdown-it-cli README.md > public/index.html
          npx markdown-it-cli dist/arch/install.md > public/arch.html

      - name: Upload public folder
        uses: actions/upload-artifact@v4
        with:
          name: public-site
          path: public/
