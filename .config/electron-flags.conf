# Electron flags configuration
# Under UWSM, ELECTRON_OZONE_PLATFORM_HINT=auto is already exported.
# Avoiding deprecated and unrecognized flags here prevents IDE startup warnings
# and allows full GPU acceleration on the Radeon GPU.
