#!/usr/bin/env bash
set -euo pipefail

if ! command -v headsetcontrol >/dev/null 2>&1; then
  exit 0
fi

status="$(headsetcontrol -b 2>/dev/null || true)"
if [[ "$status" =~ ([0-9]{1,3})% ]]; then
  echo "HS ${BASH_REMATCH[1]}%"
fi
