{
  "pulseaudio": {
    "format": "{volume}% {icon}",
    "format-bluetooth": "{volume}% {icon}",
    "format-muted": "",
    "format-icons": {
      "headphones": "",
      "handsfree": "",
      "headset": "",
      "phone": "",
      "portable": "",
      "car": "",
      "default": [
        "",
        ""
      ]
    },
    "on-click": "pactl set-default-sink $(pactl list short sinks | grep -v \"$(pactl info | grep 'Default Sink' | cut -d' ' -f3)\" | head -n1 | cut -f2)",
    "on-click-right": "pavucontrol"
  },
  "custom/headsetcontrol": {
    "format": "{}",
    "exec": "headsetcontrol -b 2>/dev/null | awk -F'Level:' '/Level/ {gsub(/[^0-9]/,\"\",$2); print $2\"% \"; exit}'",
    "tooltip": false,
    "interval": 3600
  }
}
