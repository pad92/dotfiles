- defaults:
    link:
      relink: true

- link:
    ~/.tmux.conf: .tmux.conf
    ~/.tmux: .tmux
    ~/.vim: .vim
    ~/.vimrc: .vimrc
    ~/.screenrc: .screenrc
    ~/.Xresources: .Xresources
    ~/.Xdefaults: .Xdefaults
    ~/.zshrc: .zshrc
    ~/.zshenv: .zshenv
    ~/.config/:
      glob: true
      path: .config/*
      force: true
- shell:
  - [git submodule update --init --recursive, Installing submodules]
