- defaults:
    link:
      relink: true

- link:
    ~/.Xdefaults: .Xdefaults
    ~/.Xresources: .Xresources
    ~/.fonts: .fonts
    ~/.screenrc: .screenrc
    ~/.tmux.conf: .tmux.conf
    ~/.tmux: .tmux
    ~/.vim: .vim
    ~/.vimrc: .vimrc
    ~/.zshenv: .zshenv
    ~/.zshrc: .zshrc
    ~/.config/:
      glob: true
      path: .config/*
      force: true
- shell:
  - [git submodule update --init --recursive, Installing submodules]
