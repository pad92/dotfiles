- defaults:
    link:
      relink: true

- link:
    ~/.Xresources: .Xresources
    ~/.screenrc: .screenrc
    ~/.tmux.conf: .tmux.conf
    ~/.tmux: .tmux
    ~/.vim: .vim
    ~/.vimrc: .vimrc
    ~/.xinitrc: .xinitrc
    ~/.zshenv: .zshenv
    ~/.zshrc: .zshrc
    ~/.config/:
      glob: true
      path: .config/*
      force: true
- shell:
  - [git submodule update --init --recursive, Installing submodules]
